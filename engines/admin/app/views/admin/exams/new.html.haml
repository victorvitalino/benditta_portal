.row
  .column
    %h2.ui.header
      Exames
      .sub.header 
        Formulário para criação de exames
.row
  .column
    .mini-divider
    = link_to "Retornar para listagem", clinics_path, class: 'ui button blue', data: { disable_with:  t(:disable_with)}
.divider
.row
  .column
    .ui.segment
      = render 'form'
