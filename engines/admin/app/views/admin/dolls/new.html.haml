.row
  .column
    %h2.ui.header
      Bonecas
      .sub.header
        Formulário para criação de boneca
.row
  .column
    .mini-divider
    = link_to "Retornar para listagem", dolls_path, class: 'ui button blue', data: { disable_with:  t(:disable_with)}
.divider
.row
  .column
    .ui.segment
      = render 'form'
