.row
  .column
    %h2.ui.header
      Postagens
      .sub.header
        Formulário para criação de postagem
.row
  .column
    .mini-divider
    = link_to "Retornar para listagem", posts_path, class: 'ui button blue', data: { disable_with:  t(:disable_with)}
.divider
.row
  .column
    .ui.segment
      = render 'form'
