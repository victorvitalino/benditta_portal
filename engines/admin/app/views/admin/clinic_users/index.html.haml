.row
  .column
    %h2.ui.header
      Usuários - Clínica
      .sub.header 
        Visualização de usuários 
  .column
    .mini-divider
    = link_to "Voltar para listagem", clinics_path, class: 'ui button blue', data: { disable_with:  t(:disable_with)}
    = link_to "Visualizar clínica", clinic_path(@clinic), class: 'ui button blue', data: { disable_with:  t(:disable_with)}
.divider
.row
  .column
    .ui.segment