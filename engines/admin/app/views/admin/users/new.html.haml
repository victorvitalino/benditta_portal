.row
  .column
    %h2.ui.header
      Clínicas
      .sub.header 
        Formulário para criação de usuário
.row
  .column
    .mini-divider
    = link_to t(:list_return), users_path, class: 'ui button blue', data: { disable_with:  t(:disable_with)}
.divider
.row
  .column
    .ui.segment
      = render 'form'
